<!DOCTYPE html>
<html>
  <head>
    <title>Symantec Bank</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container">

	  	<p class="notice"><%= notice %></p>
	    <p class="alert"><%= alert %></p>

	    <% if user_signed_in? %>
	    	<%= link_to destroy_user_session_path(), method: :delete do %>
	    		<btn class="logout_button">Logout</btn>
	    	<% end %>	
	    <% end %>

      <% unless user_signed_in? %>

        <p class="text-center bank-name">Symantec Bank</p>
        <p class="text-center bank-description">This bank utalizes Symantec's VIP Services.</p>
      <% end %>

    	<%= yield %>
      
    </div>
  </body>
</html>
